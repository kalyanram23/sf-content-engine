Codemap Analysis Report
Generated: 2026-07-19
Project: content-engine (stateless TypeScript ESM library)
Scan: 113 src .ts (non-test), 56 test files, 169 total src .ts
Previous scan: 2026-07-12 (103 non-test, 152 total)

=== STATUS: REGENERATION (4th scan) — UPDATE IN PLACE, no approval gate ===
The six maps in docs/CODEMAPS/ were last written on 2026-07-12 (D77-era) but sat UNCOMMITTED
in the working tree while the code moved on. Since then the theme-migration wave (D78) and
sparse-board photo growth (D79) landed. This run refreshed the maps against the current tree.

Change is TARGETED (freshness headers + a handful of lines per map — the composition surface
went from 1 composed theme to 5-on-a-shared-toolbox); it does NOT exceed the 30% "request
approval" threshold in any map, so all six were updated in place. Full before/after is
recoverable in the working tree (`git diff docs/CODEMAPS/`).

=== WHAT DROVE THE DIFF: composition went from 1 theme to 5 (D78) + sized prices + D79 ===
The prior maps described an engine where ONLY `dhaba` composed and everything else free-painted.
Now FIVE of the six themes compose, all on a shared toolbox.

  builtinVocabularies() (src/vocabularies/index.ts) registers 5:
    dhaba · bold-poster · blockframe · bazaar · bubblegum   (only `botanical` free-paints)
  All but dhaba are built on src/vocabularies/shared/ (D78):
    binding.ts        QA-exact data-item-id / data-bind escaping + bindPrices (per-size tagging)
    carousels.ts      crossfade + filmstrip bands with the reduced-motion settled frame
    registers.ts      register/type-scale math (metricsFromNumbers)
    masthead.ts       logo-box / title masthead
    contract.testkit.ts  describeVocabularyContract — the reusable per-vocabulary contract suite
  dhaba keeps PRIVATE copies of the toolbox mechanics as the untouched reference, pinned by the
  toolbox's sync tests (D78) — so a shared-kit regression is caught against a known-good baseline.

  Graph, nodes, state channels, routing and termination are STRUCTURALLY UNCHANGED. Composedness
  is still DERIVED (isComposedHtml(state.html) via the data-composed marker), never stored. The
  composed-board QA trust is unchanged in shape; only its examples generalized (dhaba's dotted-
  leader idiom is now one of several per-vocabulary price layouts).

=== FILES ADDED since last scan (17: 10 impl + 7 test) ===
  src/vocabularies/shared/{binding,carousels,contract.testkit,masthead,registers}.ts   shared toolbox
  src/vocabularies/{bold-poster,blockframe,bazaar,bubblegum}/index.ts                   4 new vocabularies
  src/adapters/bundled-themes.ts        bundledThemesDir() — package ships its own themes/ bundle
  (+ bundled-themes.test.ts, 4 per-vocabulary .test.ts, shared/binding.test.ts, shared/reference.test.ts)

=== FILES MODIFIED since last scan (impl) ===
  src/config/qa.ts                 requiredBindings default price → ["price","name"] (§4 rename target)
  src/config/layouts.ts            packed-landscape column escalation for composed boards
  src/qa/structural-checks.ts      per-size price binding verified per data-size tag
  src/ports/vocabulary-registry.ts VocabItem.sizes — sized items reach the vocabularies
  src/composition/{digest,renderer}.ts  sized-price digest + sparse-board photo growth render (D79)
  src/adapters/node-engine.ts      themesDir defaults to bundledThemesDir(); src/node.ts re-exports it
  src/adapters/openrouter/painter.ts    sized-price prompt notes
  src/vocabularies/{dhaba,bold-poster,blockframe,bazaar,bubblegum}/index.ts · shared/binding.ts

=== FILES DELETED === none

=== SNAPSHOT DIFF (vs 2026-07-12 baseline) ===
Pipeline nodes:        10 -> 10   UNCHANGED (planContent..freeze); routing/termination unchanged
State channels:        no change  (composedness still DERIVED from html, not a channel)
EnginePorts:           15 -> 15   composer? / vocabularies? still declared, still inert (see DEAD CODE)
Model roles:            5 -> 5    UNCHANGED (plan/paint/critique/repair/compose)
Config-as-data blocks: 11 -> 11   qa.requiredBindings default now price+name; layouts gained packed escalation
Composition roots:      4 -> 4    UNCHANGED
Composed vocabularies:  1 -> 5    +bold-poster, blockframe, bazaar, bubblegum (all on shared toolbox, D78)
Themes on disk:         6 -> 6    UNCHANGED; but declaring a vocabulary went 1 -> 5 (all but botanical)
Required data-bind:     price -> price + name   (D: overlay rename target, spec §4)
Sized prices:           NEW       per-size prices carry data-size tags; verified per tag in QA
Packaging:              NEW       files: dist + themes; themesDir defaults to bundledThemesDir() (D78)

=== NEW DEPENDENCIES === none (package.json deps unchanged since the prior scan)

=== EXTERNAL SERVICES ===
Unchanged: OpenRouter (LLM gateway, 5 roles incl. compose), headless Chromium (Playwright),
remote image hosts. No database / cache / queue. Stateless across calls (D15).

=== DEAD / DANGLING CODE (re-verified this scan; still open, needs an owner decision) ===
1. EnginePorts.composer / .vocabularies are declared optional but NO composition root sets them
   and NOTHING in the core reads them (only a doc comment in config/painter.ts mentions them).
   Both roots inject Composer + VocabularyRegistry straight into AutoPainter/CompositionPainter,
   so the Painter port remains the sole composition seam. The two port fields stay inert.
2. zod-to-json-schema is a direct dependency imported NOWHERE in src/ or scripts/ (Zod 4's built-in
   z.toJSONSchema replaced it). Still resolved transitively via @langchain/*, so dropping the direct
   dep is safe.
3. `npm run try:claude` is still a dangling script: src/adapters/claudecode/** and scripts/try-claude.ts
   are GITIGNORED + eslint-ignored, so a fresh clone has neither, while package.json ships the script
   and the @anthropic-ai/claude-agent-sdk devDependency. No tracked file imports it — build/verify unaffected.

=== STALENESS WARNINGS (docs not updated in 90+ days) ===
None. The repo is ~4 weeks old; every doc dates from 2026-06 or 2026-07. Content-lag notes instead:
  - CHANGES.md (2026-06-27) is a DATED ARCHIVE ("Change list - 2026-06-24") predating D28-D79.
    Not wrong, but no longer describes the engine; DECISIONS.md is the living log. Candidate for
    retitling-as-archive or retirement (carried over from the prior report — still unaddressed).
  - ARCHITECTURE.md / CLAUDE.md / DECISIONS.md are CURRENT (through D79, 2026-07-16).
  - README.md / docs/CONTRIBUTING.md were reconciled with package.json + .env.example in the same
    session as this scan (scripts table gained vocab:samples; env table already in sync).

=== NOTES ===
- docs/ is in .prettierignore, so the CODEMAPS are NOT prettier-checked (free-form ASCII tables OK).
- No src/ file was modified by this documentation work.
- The maps + README/CONTRIBUTING doc edits are UNCOMMITTED in the working tree.
