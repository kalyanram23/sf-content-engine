# Dependencies
node_modules

# Build output (tsup) + TS incremental cache
dist
*.tsbuildinfo

# Test coverage
coverage

# Engine run artifacts (playground / scripts / debug sink)
playground-output
real-output
/debug/

# Logs (npm/yarn/pnpm debug logs included)
*.log

# Environment / secrets — keep the committed template
.env
.env.*
!.env.example

# npm packaging (npm pack / publish tarball)
*.tgz

# Editor / IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store

# Claude Code — personal, not shared (also in the user's global ignore)
.claude/settings.local.json
CLAUDE.local.md
# Added by Braintrust Wizard
.braintrust.json

# Claude Agent SDK backend — shelved experiment (local-only, not shipped; excluded from verify)
src/adapters/claudecode/
scripts/try-claude.ts
claude-output/
debug-claude/

# Eval run artifacts (scripts/evals) — regenerated per run
eval-output*/
eval-smoke*/

# Design-exploration scratch workspace (HTML mockups + verify script) — kept on disk, not shipped
design-explorations/

# Quality-loop iteration archives (posters/reports per iteration; large PNGs, session-local)
eval-loop/

# vocabulary density-sample renders (vocab:samples output)
vocab-samples

# superpowers visual-companion scratch (mockups/state served to the browser)
.superpowers/
